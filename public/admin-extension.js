(function(){
  const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
  async function api(url,options={}){const response=await fetch(url,{credentials:'same-origin',...options,headers:{'Content-Type':'application/json',...(options.headers||{})}});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||data.message||'Não foi possível concluir.');return data;}
  const labels={ranking:'Página de ranking',ranking_ticker:'Tarjeta do ranking',scripts:'Playbook de Vendas',portfolio:'Portfólio',sales_routes:'Rotas comerciais',project_war:'Projeto Guerra',kommo:'Kommo',blog:'Blog',marketing:'Materiais',ideaaco:'#IdeAÇO',feed:'Feed',hero_announcements:'Comunicados no hero',profiles:'Perfis',content:'Conteúdo geral'};
  const roles={member:'Colaborador',area_manager:'Gestor da área',staff:'Staff / moderação',super_admin:'Administrador geral'};
  const dt=value=>value?new Date(value).toISOString().slice(0,16):'';
  function announcementForm(item={}){return '<form class="announcement-form" data-id="'+esc(item.id||'')+'"><div class="admin-form-head"><div><span class="section-kicker">COMUNICADO INTERNO</span><h4>'+(item.id?'Editar slide':'Novo slide')+'</h4></div><label class="toggle"><input name="active" type="checkbox" '+(item.active!==false?'checked':'')+'> Ativo</label></div><label>Título <small><span data-count="title">'+esc((item.title||'').length)+'</span>/80</small><input name="title" maxlength="80" required value="'+esc(item.title||'')+'"></label><label>Subtítulo <small><span data-count="subtitle">'+esc((item.subtitle||'').length)+'</span>/140</small><input name="subtitle" maxlength="140" value="'+esc(item.subtitle||'')+'"></label><label>Resumo <small><span data-count="body">'+esc((item.body||'').length)+'</span>/320</small><textarea name="body" maxlength="320" rows="4" required>'+esc(item.body||'')+'</textarea></label><label>Link da íntegra<input name="url" type="url" required placeholder="https://..." value="'+esc(item.url||'')+'"></label><div class="admin-date-grid"><label>Entra no ar<input name="startsAt" type="datetime-local" required value="'+esc(dt(item.starts_at))+'"></label><label>Sai do ar<input name="expiresAt" type="datetime-local" required value="'+esc(dt(item.expires_at))+'"></label></div><button class="button primary" type="submit">Salvar comunicado</button></form>';}
  function announcementPanel(items){return '<section class="management-panel"><header><div><span class="section-kicker">RH · HERO</span><h3>Comunicados internos</h3><p>Até três slides ativos. A validade retira cada comunicado automaticamente do ar.</p></div><span class="panel-counter">'+items.filter(x=>x.active&&new Date(x.expires_at)>new Date()).length+'/3 ativos</span></header><div class="announcement-grid">'+items.map(announcementForm).join('')+(items.length<3?announcementForm():'')+'</div></section>';}
  function templatesPanel(data){const year=data.year||new Date().getFullYear(),map=Object.fromEntries((data.templates||[]).map(item=>[item.event_type,item])),defaults={birthday:{subject_template:'Feliz aniversário, {{name}}! 🎉',headline:'Hoje é dia de celebrar você!',message_html:'<p>Que este novo ciclo seja repleto de saúde, alegrias e grandes conquistas.</p><p>Receba o carinho de toda a equipe do <strong>Grupo ABR</strong>.</p>'},work_anniversary:{subject_template:'{{name}}, feliz aniversário de Grupo ABR!',headline:'Uma história construída com a gente',message_html:'<p>Hoje celebramos <strong>{{years}} ano(s)</strong> da sua trajetória no Grupo ABR.</p><p>Obrigado por fazer parte dessa construção.</p>'}};return '<section class="management-panel"><header><div><span class="section-kicker">RH · E-MAILS AUTOMÁTICOS</span><h3>Templates de celebração '+year+'</h3><p>Use <b>{{name}}</b> para o nome e <b>{{years}}</b> para o tempo de empresa.</p></div></header><div class="template-grid">'+[['birthday','Aniversário natalício'],['work_anniversary','Aniversário de empresa']].map(([type,label])=>{const item=map[type]||defaults[type];return '<form class="celebration-template-form" data-type="'+type+'"><h4>'+label+'</h4><input type="hidden" name="templateYear" value="'+year+'"><label>Assunto<input name="subjectTemplate" maxlength="180" required value="'+esc(item.subject_template)+'"></label><label>Chamada principal<input name="headline" maxlength="120" required value="'+esc(item.headline)+'"></label><label>Mensagem em HTML<textarea name="messageHtml" maxlength="6000" rows="7" required>'+esc(item.message_html)+'</textarea></label><label class="toggle"><input name="active" type="checkbox" '+(item.active!==false?'checked':'')+'> Template ativo</label><button class="button primary" type="submit">Salvar template</button></form>';}).join('')+'</div></section>';}  function areasPanel(areas){const resources=Object.entries(labels);return '<section class="management-panel access-matrix-panel"><header><div><span class="section-kicker">ADMINISTRAÇÃO</span><h3>Matriz de acessos</h3><p>Marque os módulos liberados para cada área. Permissões administrativas existentes são preservadas.</p></div></header><form id="areaCreateForm" class="area-create"><input name="name" maxlength="80" required placeholder="Nome da área"><input name="description" maxlength="240" placeholder="Descrição breve"><button class="button primary" type="submit">Criar área</button></form><div class="access-matrix-wrap"><table class="access-matrix"><thead><tr><th>Área</th>'+resources.map(([,label])=>'<th><span title="'+esc(label)+'">'+esc(label)+'</span></th>').join('')+'<th>Salvar</th></tr></thead><tbody>'+areas.map(area=>'<tr data-area="'+area.id+'"><th><b>'+esc(area.name)+'</b><small>'+esc(area.description||'')+'</small></th>'+resources.map(([resource,label])=>{const level=area.permissions[resource]||'none';return '<td><label class="matrix-check" title="'+esc(area.name)+' · '+esc(label)+'"><input type="checkbox" data-resource="'+resource+'" data-level="'+level+'" '+(level!=='none'?'checked':'')+'><span aria-hidden="true"></span><small>'+(level==='manage'?'Ger.':'')+'</small></label></td>';}).join('')+'<td><button class="matrix-save area-save" type="button" aria-label="Salvar acessos de '+esc(area.name)+'">Salvar</button></td></tr>').join('')+'</tbody></table></div><div class="matrix-legend"><span><i class="legend-check"></i> Acesso liberado</span><span><b>Ger.</b> Permissão de gerenciamento preservada</span></div></section>';}
  function usersPanel(users,areas){return '<section class="management-panel"><header><div><span class="section-kicker">ADMINISTRAÇÃO</span><h3>Perfis cadastrados</h3><p>Associe pessoas às áreas, defina o papel e suspenda acessos quando necessário.</p></div></header><div class="profile-admin-list">'+users.map(user=>'<form class="profile-admin-row" data-user="'+esc(user.user_id)+'"><div><b>'+esc(user.display_name)+'</b><small>'+esc(user.email)+'</small></div><label>Área<select name="areaId"><option value="">Sem área</option>'+areas.map(area=>'<option value="'+area.id+'" '+(user.area_id===area.id?'selected':'')+'>'+esc(area.name)+'</option>').join('')+'</select></label><label>Papel<select name="role">'+Object.entries(roles).map(([value,label])=>'<option value="'+value+'" '+(user.role===value?'selected':'')+'>'+label+'</option>').join('')+'</select></label><label class="toggle"><input name="active" type="checkbox" '+(user.active!==false?'checked':'')+'> Ativo</label><button class="button secondary" type="submit">Salvar</button></form>').join('')+'</div></section>';}
  async function mount(){const host=document.querySelector('#authArea');if(!host||host.dataset.adminMounted==='1'||!host.querySelector('.member-layout'))return;host.dataset.adminMounted='1';try{const access=await api('/api/access/me');let html='';if(access.canManageAnnouncements){const [result,templates]=await Promise.all([api('/api/announcements'),api('/api/celebration-templates')]);html+=announcementPanel(result.announcements||[])+templatesPanel(templates);}if(access.isSuperAdmin){const [areas,users]=await Promise.all([api('/api/admin/areas'),api('/api/admin/users')]);html+=areasPanel(areas.areas||[])+usersPanel(users.users||[],areas.areas||[]);}if(html){const dashboard=document.createElement('div');dashboard.className='management-dashboard';dashboard.innerHTML=html;host.appendChild(dashboard);}if(access.canModerateFeed)document.querySelectorAll('.feed-post[data-post-id]').forEach(post=>{if(post.querySelector('.feed-moderate'))return;const button=document.createElement('button');button.type='button';button.className='feed-moderate';button.textContent='Ocultar publicação';button.dataset.postId=post.dataset.postId;post.querySelector('header')?.appendChild(button);});}catch(error){console.warn('Painel administrativo indisponível:',error.message);}}
  document.addEventListener('input',event=>{const form=event.target.closest('.announcement-form');if(!form)return;const count=form.querySelector('[data-count="'+event.target.name+'"]');if(count)count.textContent=event.target.value.length;});
  document.addEventListener('click',async event=>{const save=event.target.closest('.area-save');if(save){const row=save.closest('[data-area]'),permissions={};row.querySelectorAll('[data-resource]').forEach(input=>permissions[input.dataset.resource]=input.checked?(input.dataset.level==='manage'?'manage':'view'):'none');try{await api('/api/admin/areas',{method:'PUT',body:JSON.stringify({areaId:row.dataset.area,permissions})});toast('Permissões da área atualizadas.');}catch(error){toast(error.message);}return;}const moderation=event.target.closest('.feed-moderate');if(moderation){try{await api('/api/admin/feed',{method:'PUT',body:JSON.stringify({postId:moderation.dataset.postId,hidden:true})});moderation.closest('.feed-post').remove();toast('Publicação ocultada do feed.');}catch(error){toast(error.message);}}});
  document.addEventListener('submit',async event=>{const form=event.target;if(!['areaCreateForm'].includes(form.id)&&!form.matches('.profile-admin-row,.announcement-form,.celebration-template-form'))return;event.preventDefault();const values=Object.fromEntries(new FormData(form).entries()),button=form.querySelector('button[type="submit"]'),original=button.textContent;button.disabled=true;button.textContent='Salvando...';try{if(form.id==='areaCreateForm')await api('/api/admin/areas',{method:'POST',body:JSON.stringify(values)});else if(form.matches('.profile-admin-row'))await api('/api/admin/users',{method:'PUT',body:JSON.stringify({...values,userId:form.dataset.user,active:form.elements.active.checked})});else if(form.matches('.celebration-template-form'))await api('/api/celebration-templates',{method:'PUT',body:JSON.stringify({...values,eventType:form.dataset.type,active:form.elements.active.checked})});else await api('/api/announcements',{method:form.dataset.id?'PUT':'POST',body:JSON.stringify({...values,id:form.dataset.id||undefined,active:form.elements.active.checked})});toast('Alteração salva.');document.querySelector('#authArea').dataset.adminMounted='0';if(typeof renderArea==='function')await renderArea();else location.reload();}catch(error){toast(error.message);button.disabled=false;button.textContent=original;}});
  new MutationObserver(()=>mount()).observe(document.querySelector('#playbookContent'),{childList:true,subtree:true});mount();
})();