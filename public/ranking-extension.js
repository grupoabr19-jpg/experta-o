(function(){
  if (!sections.some(item => item.id === 'ranking')) sections.splice(1, 0, { id:'ranking', icon:'R$', label:'Ranking' });
  meta.ranking = ['02','Ranking comercial','Desempenho proporcional às metas em reais e toneladas por equipe e vendedor.'];
  const percentage = item => Number.isFinite(Number(item.attainment)) ? Number(item.attainment) : null;
  const ordered = items => items.filter(item => percentage(item)!==null).sort((a,b)=>(a.position||0)-(b.position||0));
  const identity = (item,kind) => kind==='team' ? `<strong>${item.name||'Equipe a definir'}</strong><small>${item.route||'Região a definir'}</small>` : `<strong>${item.name||'Vendedor a definir'}</strong><small>${item.route||'Região a definir'}</small>`;
  const tons = value => value!=null?Number(value).toLocaleString('pt-BR')+' Ton.':'&mdash;';
  const table = (items,kind) => { const rows=ordered(items); if(!rows.length)return '<div class="ranking-empty">Aguardando dados da planilha de ranking.</div>'; const label=kind==='team'?'Região / equipe':'Vendedor / região'; return `<div class="ranking-table"><div class="ranking-row ranking-head"><span>Pos.</span><span>${label}</span><span>Realizado</span><span>Meta</span><span>% da meta</span><span>Status</span></div>${rows.map(x=>`<div class="ranking-row"><b>${x.position}&ordm;</b><span>${identity(x,kind)}</span><span>${tons(x.tons)}</span><span>${tons(x.targetTons)}</span><em>${x.attainment.toFixed(1)}%</em><span>${x.status||'&mdash;'}</span></div>`).join('')}</div>`; };
  const populate = data => { const teams=data.teams||data.regions||[],sellers=data.sellers||[],m=data.management||{}; for(let i=cards.length-1;i>=0;i--)if(cards[i].section==='ranking')cards.splice(i,1); cards.push({id:'ranking-leadership',section:'ranking',audience:'todos',title:'Liderança comercial',tags:['gestão'],html:`<div class="leadership"><span><small>Direção geral · responsável pela meta geral</small><b>${m.generalManager||'&mdash;'}</b></span><span><small>Gerente de vendas · supervisor de Rafael · assistente de Marcelo</small><b>${m.salesManager||'&mdash;'}</b></span><span><small>Supervisor de vendas · todas as rotas</small><b>${m.salesSupervisor||'&mdash;'}</b></span></div>`},{id:'ranking-teams',section:'ranking',audience:'todos',title:'Ranking por equipe / rota',tags:['ranking'],html:table(teams,'team')},{id:'ranking-sellers',section:'ranking',audience:'todos',title:'Ranking por vendedor',tags:['ranking'],html:table(sellers,'seller')}); renderNav(); renderContent(); observeSections(); };
  loadRanking = async () => { const track=document.querySelector('#tickerTrack'); track.style.animation='none'; track.offsetHeight; track.style.animation=''; track.innerHTML='<span>Atualizando ranking...</span>'; try { const response=await fetch('/api/ranking?channel=ranking'); if(!response.ok)throw new Error(); const data=await response.json(); populate(data); const teams=data.teams||data.regions||[],sellers=data.sellers||[]; const entries=[...ordered(teams).map(x=>({...x,type:'Equipe',display:`${x.name} / ${x.route||'Região a definir'}`})),...ordered(sellers).map(x=>({...x,type:'Vendedor',display:`${x.name} / ${x.route||'Região a definir'}`}))]; if(!entries.length){track.innerHTML='<span>Ranking aguardando dados da planilha.</span>';return;} const html=entries.map(x=>`<span class="ticker-item"><b>${x.position}&ordm;</b> ${x.type}: ${x.display} <em>${x.attainment.toFixed(1)}% da meta</em></span>`).join(''); track.innerHTML=html+html; } catch { track.innerHTML='<span>Ranking temporariamente indisponível</span>'; } };
  loadRanking();
  setInterval(loadRanking,300000);
})();